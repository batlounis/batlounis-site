<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or story.title | striptags }} | Batlounis</title>
    <meta name="description" content="{{ description or story.description or 'Default site description.' }}">
    <meta property="og:title" content="{{ title or story.title | striptags }} | Batlounis" />
    <meta property="og:description" content="{{ description or story.description or 'Default site description.' }}" />
    <meta property="og:url" content="https://batlounis.com{{ page.url }}" />
    <meta property="og:type" content="article" />
    <link rel="icon" href="/assets/public/favicon.png" type="image/png">

    {# <meta property="og:image" content="https://batlounis.com/assets/images_cropped/{{ story.slug }}/{{ image }}" /> #}

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="{{ title or story.title | striptags }} | Batlounis" />
    <meta name="twitter:description" content="{{ description or story.description or 'Default site description.' }}" />
    {# <meta name="twitter:image" content="https://batlounis.com/assets/images_cropped/{{ story.slug }}/{{ firstImage.filename }}" /> #}
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
      .grecaptcha-badge {
        visibility: hidden;
      }
    </style>
    
    <script>
      if (location.hostname !== 'localhost') {
        var gtagScript = document.createElement('script');
        gtagScript.async = true;
        gtagScript.src = 'https://www.googletagmanager.com/gtag/js?id=G-40TNB9Y484';
        document.head.appendChild(gtagScript);

        window.dataLayer = window.dataLayer || [];
        function gtag(){ dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', 'G-40TNB9Y484');
      }
    </script>
</head>
<body class="bg-gradient-to-b from-gray-100 to-white text-gray-900">

    {% include "header.njk" %}

    {{ content | safe }}

    {% include "form-stories-license.njk" %}

    {% include "footer.njk" %}

    <div class="mt-12 mb-12 text-center">
      <p class="text-xs text-gray-500">
        This site is protected by reCAPTCHA and the Google
        <a href="https://policies.google.com/privacy" target="_blank" rel="noopener noreferrer" class="underline">Privacy Policy</a> and
        <a href="https://policies.google.com/terms" target="_blank" rel="noopener noreferrer" class="underline">Terms of Service</a> apply.
      </p>
    </div>
</body>
</html>
