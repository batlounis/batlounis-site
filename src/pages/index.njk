---
title: "Stories"
layout: "layout.njk"
permalink: "/index.html"
---


<main class="text-center mt-16 px-6">
  <h2 class="text-5xl md:text-6xl tracking-tight font-bold mt-4">
      Hey, I'm Salim!
  </h2>
  
  <p class="mt-6 text-left text-base tracking-tight text-black max-w-lg mx-auto leading-snug">
      Step into a curated world of unique stock photography where everyday scenes meet thoughtful storytelling. From vibrant home-cooked dishes to quiet travel details, my collection celebrates authenticity, diversity, and the beauty of the unscripted. 
  </p>

  <p class="mt-12 text-sm tracking-widest text-gray-600 uppercase">Authentic Moments & Uncommon Perspectives</p>
</main>

<section class="px-4 max-w-6xl mx-auto mt-16">
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
    {% for page in collections.stories %}
      {% set slug = page.data.story.slug %}
      {% set gridRows = grid | filterBySlug(slug) %}
      {% set firstImage = gridRows[0] and gridRows[0].images[0] %}
      {% set photo = images | findPhotoByFile(slug, firstImage.filename) %}
      
      <a href="{{ page.url }}" class="block group">
        <div class="aspect-[1/1] overflow-hidden rounded-lg shadow-md">
          <img
            class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
            src="/assets/images_cropped/{{ slug }}/{{ firstImage.filename }}"
            alt="{{ photo.alt_text | default(page.data.story.title) }}"
          >
        </div>
        <h2 class="mt-4 text-lg font-semibold text-center">
          {{ page.data.story.title | safe}}
        </h2>
      </a>
    {% endfor %}
  </div>
</section>


  <p class="mt-12 mb-12 text-left text-base tracking-tight text-black max-w-lg mx-auto leading-snug">
      Whether you're looking for striking compositions or subtle emotions, you'll find visuals here that go beyond cliché. Made with curiosity, crafted with care.
  </p>