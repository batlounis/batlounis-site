---
title: "Authentic stock photos & uncommon perspectives"
description: From vibrant home-cooked dishes to quiet travel details, my collection celebrates authenticity, diversity, and the beauty of the unscripted.
layout: "layout.njk"
permalink: "/index.html"
hide_tagline: true
---

<main class="text-center mt-16 px-6">
  <p class="text-sm tracking-widest text-gray-500 uppercase mb-4">Authentic stock photos & uncommon perspectives</p>

  <h2 class="text-5xl md:text-6xl tracking-tight font-bold">
    Hey, I'm Salim!
  </h2>
  
  <p class="mt-6 text-left text-base tracking-tight text-black max-w-md mx-auto leading-relaxed">
    Step into a curated world of unique stock photography where everyday scenes meet thoughtful storytelling. 
  </p>
  <p class="mt-4 text-left text-base tracking-tight text-black max-w-md mx-auto leading-relaxed">
    From vibrant home-cooked dishes to quiet travel details, my collection celebrates authenticity, diversity, and the beauty of the unscripted. 
  </p>
</main>

<section class="px-4 max-w-6xl mx-auto mt-16">
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
    {% for page in collections.stories %}
      {% set slug = page.data.story.slug %}
      {% set gridRows = grid | filterBySlug(slug) %}
      {% set firstImage = gridRows[0] and gridRows[0].images[0] %}
      {% set photo = images | findPhotoByFile(slug, firstImage.filename) %}
      
      <a href="{{ page.url }}" class="block group">
        <div class="aspect-[1/1] overflow-hidden rounded-lg shadow-md">
        {% if firstImage and firstImage.filename and firstImage.filename.endsWith('.mp4') %}          <video
            class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
            src="/assets/images_cropped/{{ slug }}/{{ firstImage.filename }}"
            autoplay muted loop playsinline
          ></video>
        {% else %}
          <img
            class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
            src="/assets/images_cropped/{{ slug }}/{{ firstImage.filename }}"
            alt="{{ photo.alt_text | default(page.data.story.title) }}"
          >
        {% endif %}
        </div>
        <p class="mt-3 text-base text-gray-800 text-center leading-snug">
          {{ page.data.story.title | safe }}
        </p>
      </a>
    {% endfor %}
  </div>
</section>


<section class="bg-gray mt-24 py-12 px-6">
  <p class="text-center text-base tracking-tight text-gray-700 max-w-xl mx-auto leading-relaxed">
    Whether you're looking for striking compositions or subtle emotions, you'll find visuals here that go beyond cliché. Made with curiosity, crafted with care.
  </p>
</section>